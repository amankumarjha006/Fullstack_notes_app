import { SignupForm } from "@/components/auth/signup-form";

export const metadata = {
  title: "Sign Up — AI Notes Workspace",
  description: "Create an account for your collaborative AI notes workspace.",
};

export default function SignupPage() {
  return <SignupForm />;
}
