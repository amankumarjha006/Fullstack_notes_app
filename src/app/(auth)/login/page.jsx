import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Sign In — AI Notes Workspace",
  description: "Sign in to your collaborative AI notes workspace.",
};

export default function LoginPage() {
  return <LoginForm />;
}
